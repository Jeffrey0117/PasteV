# AI Generation Mode - 功能規格書

## 概述

AI Generation Mode 是一個全新的內容生成模式，讓使用者可以透過 AI 自動生成社群媒體圖文卡片（如 Instagram 知識型貼文）。使用者只需輸入主題或內容，AI 會自動：
1. 生成/整理文字內容
2. 搜尋並選擇適合的背景圖片
3. 套用模板生成最終圖片

## 目標用戶

- 社群媒體經營者
- 知識型內容創作者
- 想快速製作圖文卡片的用戶

## 功能流程

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  1. 輸入    │ -> │  2. 確認    │ -> │  3. 生成    │ -> │  4. 預覽    │
│  Input      │    │  Confirm    │    │  Generate   │    │  Preview    │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

### Step 1: 輸入 (Input)

使用者可選擇兩種輸入方式：

#### 方式 A：主題生成
- 輸入主題關鍵字（如：「VS Code 插件推薦」）
- 選擇張數（1-10 張）
- AI 自動生成內容

#### 方式 B：內容整理
- 貼上已有的文字內容
- AI 自動拆分成多張卡片
- 可指定張數或讓 AI 決定

#### 輸入參數
```typescript
interface AIGenerationInput {
  mode: 'topic' | 'content';

  // 主題模式
  topic?: string;           // 主題關鍵字
  slideCount?: number;      // 指定張數 (1-10)

  // 內容模式
  rawContent?: string;      // 原始文字內容

  // 共用設定
  style: 'informative' | 'tips' | 'listicle' | 'story';  // 風格
  language: 'zh-TW' | 'zh-CN' | 'en';  // 語言
  includeImages: boolean;   // 是否自動配圖
}
```

### Step 2: 確認 (Confirm)

AI 生成內容後，顯示預覽讓使用者確認/編輯：

```typescript
interface GeneratedContent {
  slides: SlideContent[];
  suggestedTemplate: string;  // 建議的模板 ID
}

interface SlideContent {
  id: string;
  title: string;           // 標題
  subtitle?: string;       // 副標題
  body: string;            // 主要內容
  bulletPoints?: string[]; // 要點列表
  footnote?: string;       // 註腳
  suggestedImage?: {       // AI 建議的圖片
    query: string;         // 搜尋關鍵字
    url?: string;          // 預覽 URL
  };
}
```

#### 確認頁功能
- 編輯每張卡片的文字內容
- 調整卡片順序（拖曳）
- 新增/刪除卡片
- 重新生成單張內容
- 選擇/更換背景圖片

### Step 3: 生成 (Generate)

確認內容後，開始生成圖片：

1. **圖片搜尋**（如果啟用自動配圖）
   - 使用 Unsplash API 搜尋免費圖片
   - 或使用 Pexels API
   - 根據每張卡片的內容關鍵字搜尋

2. **模板套用**
   - 套用預設模板或使用者選擇的模板
   - 將文字內容填入對應欄位

3. **圖片合成**
   - 使用 Canvas API 合成最終圖片
   - 支援批次生成

### Step 4: 預覽 (Preview)

- 顯示所有生成的圖片
- 支援單張/全部下載
- 可返回修改

## 技術架構

### 前端組件

```
client/src/components/AIGenMode/
├── AIGenModePage.tsx        # 主頁面
├── AIGenModePage.css        # 樣式
├── InputStep.tsx            # Step 1: 輸入
├── ConfirmStep.tsx          # Step 2: 確認內容
├── GenerateStep.tsx         # Step 3: 生成中
├── PreviewStep.tsx          # Step 4: 預覽結果
└── components/
    ├── SlideEditor.tsx      # 單張卡片編輯器
    ├── ImagePicker.tsx      # 圖片選擇器
    └── TemplateSelector.tsx # 模板選擇器
```

### 後端 API

```
server/src/routes/
├── ai-generate.ts           # AI 內容生成
└── image-search.ts          # 圖片搜尋

server/src/services/
├── contentGenerator.ts      # DeepSeek 內容生成
└── imageSearchService.ts    # Unsplash/Pexels API
```

### API Endpoints

#### POST /api/ai-generate/content
生成內容（Step 1 -> Step 2）

```typescript
// Request
{
  mode: 'topic' | 'content',
  topic?: string,
  rawContent?: string,
  slideCount?: number,
  style: string,
  language: string
}

// Response
{
  slides: SlideContent[],
  suggestedTemplate: string
}
```

#### GET /api/images/search
搜尋圖片

```typescript
// Request
GET /api/images/search?q=coding&count=5

// Response
{
  images: [
    {
      id: string,
      url: string,
      thumbnailUrl: string,
      author: string,
      source: 'unsplash' | 'pexels'
    }
  ]
}
```

#### POST /api/ai-generate/render
生成最終圖片

```typescript
// Request
{
  slides: SlideContent[],
  templateId: string,
  canvasSettings: CanvasSettings
}

// Response
{
  images: [
    {
      slideId: string,
      imageData: string  // base64
    }
  ]
}
```

## DeepSeek Prompt 設計

### 主題生成 Prompt

```
你是一個社群媒體內容專家。請根據以下主題生成 Instagram 風格的知識型圖文內容。

主題：{topic}
張數：{slideCount}
風格：{style}
語言：{language}

請生成以下格式的 JSON：
{
  "slides": [
    {
      "title": "標題（簡短有力，10字內）",
      "subtitle": "副標題（可選）",
      "body": "主要內容（50-100字）",
      "bulletPoints": ["要點1", "要點2"],
      "imageKeywords": ["搜尋圖片用的英文關鍵字"]
    }
  ]
}

注意：
1. 第一張應該是封面，標題要吸引人
2. 內容要有價值、易讀
3. 最後一張可以是總結或 CTA
4. imageKeywords 使用英文，適合搜尋免費圖庫
```

### 內容整理 Prompt

```
你是一個社群媒體內容專家。請將以下內容整理成 Instagram 風格的多張圖文卡片。

原始內容：
{rawContent}

目標張數：{slideCount}（如果未指定，請自行決定最適合的張數）

請生成以下格式的 JSON：
{
  "slides": [...],
  "recommendedSlideCount": number
}

注意：
1. 保留原始內容的核心資訊
2. 適當拆分成易讀的段落
3. 每張卡片內容獨立但有連貫性
```

## 圖片來源 API

### Unsplash API（推薦）
- 免費額度：50 requests/hour
- 高品質圖片
- 需註冊取得 Access Key

```typescript
// .env
UNSPLASH_ACCESS_KEY=your_access_key

// API 調用
GET https://api.unsplash.com/search/photos?query=coding&per_page=5
Headers: Authorization: Client-ID {ACCESS_KEY}
```

### Pexels API（備選）
- 免費額度：200 requests/month
- 需註冊取得 API Key

```typescript
// .env
PEXELS_API_KEY=your_api_key

// API 調用
GET https://api.pexels.com/v1/search?query=coding&per_page=5
Headers: Authorization: {API_KEY}
```

## UI/UX 設計

### 輸入頁面

```
┌────────────────────────────────────────────────────┐
│  AI 生成模式                                        │
├────────────────────────────────────────────────────┤
│                                                    │
│  ┌──────────────┐  ┌──────────────┐               │
│  │ 🎯 主題生成  │  │ 📝 內容整理  │               │
│  └──────────────┘  └──────────────┘               │
│                                                    │
│  ┌────────────────────────────────────────────┐   │
│  │                                            │   │
│  │  輸入主題或貼上內容...                      │   │
│  │                                            │   │
│  │                                            │   │
│  └────────────────────────────────────────────┘   │
│                                                    │
│  張數: [  5  ] ▼    風格: [ 知識型 ] ▼            │
│                                                    │
│  ☑ 自動配圖                                       │
│                                                    │
│            [ 開始生成 ]                            │
│                                                    │
└────────────────────────────────────────────────────┘
```

### 確認頁面

```
┌────────────────────────────────────────────────────┐
│  確認內容                           [ 重新生成 ]   │
├────────────────────────────────────────────────────┤
│                                                    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐  │
│  │  1/5    │ │  2/5    │ │  3/5    │ │  4/5    │  │
│  │ (預覽)  │ │ (預覽)  │ │ (預覽)  │ │ (預覽)  │  │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘  │
│                                                    │
│  ┌────────────────────────────────────────────┐   │
│  │ 編輯第 1 張                                │   │
│  │                                            │   │
│  │ 標題: [Code Spell Checker              ]   │   │
│  │ 副標題: [VS Code 必裝插件               ]   │   │
│  │ 內容:                                      │   │
│  │ [它能幫助你捕捉程式碼中的拼字錯誤...]     │   │
│  │                                            │   │
│  │ 背景圖: [選擇圖片] [🔍 搜尋]              │   │
│  └────────────────────────────────────────────┘   │
│                                                    │
│            [ ← 上一步 ]  [ 開始生成 → ]           │
│                                                    │
└────────────────────────────────────────────────────┘
```

## 模板系統

### 預設模板

1. **知識卡片** - 適合教學內容
   - 大標題 + 內文 + 圖示

2. **清單型** - 適合推薦/列表
   - 序號 + 項目名稱 + 說明

3. **引言型** - 適合金句/名言
   - 大字引言 + 出處

4. **對比型** - 適合比較
   - 左右分割佈局

### 模板結構

```typescript
interface Template {
  id: string;
  name: string;
  category: 'informative' | 'list' | 'quote' | 'comparison';
  fields: FieldTemplate[];
  canvasSettings: CanvasSettings;
  defaultStyles: {
    backgroundColor: string;
    accentColor: string;
    fontFamily: string;
  };
}
```

## 開發階段

### Phase 1: MVP（1-2 週）
- [ ] 基本 UI 框架
- [ ] DeepSeek 內容生成 API
- [ ] 簡單的內容編輯
- [ ] 使用現有的 Preview 組件輸出

### Phase 2: 圖片整合（1 週）
- [ ] Unsplash API 整合
- [ ] 圖片選擇器 UI
- [ ] 背景圖片自動搜尋

### Phase 3: 模板系統（1 週）
- [ ] 模板選擇器
- [ ] 預設模板設計
- [ ] 自定義模板支援

### Phase 4: 優化（持續）
- [ ] 生成品質優化
- [ ] 更多模板
- [ ] 批次下載功能
- [ ] 歷史記錄

## 注意事項

1. **API 額度管理**
   - Unsplash 免費額度有限，需做好緩存
   - DeepSeek 調用需控制頻率

2. **圖片版權**
   - 使用 Unsplash/Pexels 免費圖片需標註來源
   - 考慮在圖片上加上來源浮水印

3. **內容安全**
   - DeepSeek 生成的內容需過濾敏感詞
   - 用戶輸入需做基本驗證

4. **效能**
   - 圖片搜尋結果緩存
   - 生成過程顯示進度
